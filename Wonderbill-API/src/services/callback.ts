import { Provider } from "../interfaces/provider";

const callbackData = (requestBody: any) => {
    let requestData: [Provider] = requestBody;

    return requestData;
}

export default callbackData;